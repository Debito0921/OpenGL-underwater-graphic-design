#ifndef TEXTURE_H
#define TEXTURE_H


class texture
{
    public:
        texture();
        virtual ~texture();
        texture(const texture& other);
        texture& operator=(const texture& other);

    protected:

    private:
};

#endif // TEXTURE_H
